# Local development only — copy to .env and fill in (optional).
# On Railway, set these in the project Variables tab instead.

AUTO_APPROVE=false

# Paste full JSON file contents (single line is fine):
# GOOGLE_CREDENTIALS_JSON=
# GOOGLE_TOKEN_JSON=
